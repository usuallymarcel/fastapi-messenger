```
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

DATABASE_URL=

PGADMIN_DEFAULT_EMAIL=
PGADMIN_DEFAULT_PASSWORD=
PGADMIN_CONFIG_SERVER_MODE="True"

API_URL="" e.g http://localhost:8000 for dev 
```


```
TODO:
add group invites
add public/private friends and groups

if public make them searchable
```